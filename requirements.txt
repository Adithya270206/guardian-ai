fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pyyaml>=6.0
numpy>=1.24.0
sentence-transformers>=2.2.2
pydantic>=2.0
requests>=2.31.0
# ML threat detection (DistilBERT)
# Required: torch, transformers, scikit-learn
# Optional: datasets (only needed if using HuggingFace dataset utilities)
torch>=2.0.0
transformers>=4.35.0
scikit-learn>=1.3.0
# datasets>=2.14.0  # optional

# Incident Reporting
reportlab>=4.0.0
